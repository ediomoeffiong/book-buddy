# Example environment variables for production / docker-compose
# Do NOT commit real secrets. Use your deployment platform secret store.

# Database (Postgres) - JDBC URL format
DATABASE_URL=jdbc:postgresql://db:5432/bookbuddy
DATABASE_USERNAME=bookbuddy
DATABASE_PASSWORD=bookbuddypass

# JWT secret (must be at least 32 bytes for HS256 when not base64)
JWT_SECRET=ReplaceThisWithASecureRandomStringAtLeast32Chars

# Google Books API key (optional)
GOOGLE_BOOKS_API_KEY=

# Spring profile to activate in production
SPRING_PROFILES_ACTIVE=prod
