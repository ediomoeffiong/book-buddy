# Example environment variables for production / docker-compose
# Do NOT commit real secrets. Use your deployment platform secret store.

# PostgreSQL Database Configuration
# Format: jdbc:postgresql://host:port/database?sslmode=require&tcpKeepAlives=true
DATABASE_URL=jdbc:postgresql://localhost:5432/bookbuddy?sslmode=require&tcpKeepAlives=true
DATABASE_USERNAME=bookbuddy_user
DATABASE_PASSWORD=your_secure_password_here

# JWT secret (must be at least 32 bytes for HS256)
# Generate: openssl rand -base64 32
JWT_SECRET=ReplaceThisWithASecureRandomStringAtLeast32Characters

# Google Books API key (optional, get from Google Cloud Console)
GOOGLE_BOOKS_API_KEY=your_google_books_api_key_here

# Spring profile to activate in production
SPRING_PROFILES_ACTIVE=prod

# Optional: Server configuration
SERVER_PORT=8080
SERVER_SERVLET_CONTEXT_PATH=/

# Optional: Logging level
LOGGING_LEVEL_COM_BOOKBUDDY=INFO
LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_SECURITY=WARN